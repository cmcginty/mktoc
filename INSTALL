mktoc v1.0 // (c) 2008, Patrick C. McGinty, mktoc[@]tuxcoder[dot]com

INSTALL:
   1) Copy mktoc.py to an appropriate 'bin' directory (i.e. ~/bin,
      /usr/local/bin, /usr/bin)

   2) Make sure file has execute permissions.

      chmod a+x ..../bin/mktoc.py

   3) View README for usage instructions.
